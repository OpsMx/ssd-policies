id: 365
type: PolicyDefinition
metadata:
  policyName: "AWS:EC2:security-group-opens-all-ports"
  category: "CSPM"
  stage: "deploy"
  description: "Security Group Opens All Ports"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Replace the all-ports rule with specific inbound rules for only the required ports and protocols. Audit the attached resources to identify required ports and create targeted rules for each legitimate traffic flow."
