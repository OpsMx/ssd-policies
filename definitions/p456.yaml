id: 456
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:assume-role-policy-allows-all"
  category: "CSPM"
  stage: "deploy"
  description: "AssumeRole Policy Allows All Principals"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Never use Principal: '*' or Principal: {'AWS': '*'} without restrictive conditions. Specify exact AWS account IDs or role ARNs that should be allowed to assume the role."
