id: 466
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:inline-role-policy-allows-sts-AssumeRole"
  category: "CSPM"
  stage: "deploy"
  description: "Inline role Policy Allows sts:AssumeRole For All Resources"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Modify the inline role policy to restrict sts:AssumeRole to specific role ARNs required for the role's function. Define explicit target role ARNs or use condition keys like aws:PrincipalOrgID to limit the scope."
