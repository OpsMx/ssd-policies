id: 420
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:user-with-multiple-access-keys"
  category: "CSPM"
  stage: "deploy"
  description: "User with Multiple API Keys"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Identify which access key is actively being used via IAM credential reports and CloudTrail logs, then deactivate and delete the unused key. Use IAM roles for applications instead of long-lived access keys where possible."
