id: 460
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:managed-policy-allows-sts-AssumeRole"
  category: "CSPM"
  stage: "deploy"
  description: "Managed Policy Allows sts:AssumeRole For All Resources"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Modify the managed policy to restrict sts:AssumeRole to specific role ARNs that are legitimately required. Enumerate the specific roles needed and list their ARNs in the Resource element, or use condition keys to limit assumable roles."
