id: 352
type: PolicyDefinition
metadata:
  policyName: "AWS:EC2:security-group-opens-PostgreSQL-port-to-all"
  category: "CSPM"
  stage: "deploy"
  description: "Security Group Opens PostgreSQL Port to All"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Restrict PostgreSQL (port 5432) access to application server security groups or specific trusted IP ranges. Move databases to private subnets and use security groups to allow access only from authorized application tiers."
