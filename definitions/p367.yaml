id: 367
type: PolicyDefinition
metadata:
  policyName: "AWS:EC2:ebs-volume-not-encrypted"
  category: "CSPM"
  stage: "deploy"
  description: "EBS Volume Not Encrypted"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Enable EBS encryption for data-at-rest protection. Note: Existing unencrypted volumes cannot be encrypted directly. Create an encrypted snapshot, then create a new volume from it and replace the original."
