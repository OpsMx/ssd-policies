id: 467
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:inline-role-policy-allows-NotActions"
  category: "CSPM"
  stage: "deploy"
  description: "Inline role Policy Allows NotActions"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Replace the NotAction statement with explicit Action statements listing only the required permissions. Audit the role's actual permission usage and define a specific allowlist that matches the role's intended function."
