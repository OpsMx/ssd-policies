id: 425
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:root-account-with-active-keys"
  category: "CSPM"
  stage: "deploy"
  description: "Root Account Has Active Keys"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Delete the root account access keys immediately. The root account should never be used for daily operations. Create IAM users or roles with appropriate permissions instead."
