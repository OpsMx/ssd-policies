id: 451
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:inline-group-policy-allows-iam-PassRole"
  category: "CSPM"
  stage: "deploy"
  description: "Inline group Policy Allows iam:PassRole For All Resources"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Restrict iam:PassRole to specific role ARNs using a condition. Unrestricted PassRole combined with service actions (ec2:RunInstances, lambda:CreateFunction) enables privilege escalation by passing high-privilege roles to services."
