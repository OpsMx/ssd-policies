id: 376
type: PolicyDefinition
metadata:
  policyName: "AWS:VPC:default-network-acls-allow-all-egress"
  category: "CSPM"
  stage: "deploy"
  description: "Network ACLs Allow All egress Traffic (default)"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Review NACL rules and remove overly permissive allow-all rules. NACLs are stateless, so ensure both inbound and outbound rules follow least privilege. Consider using security groups as the primary control since they are stateful."
