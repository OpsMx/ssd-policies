id: 380
type: PolicyDefinition
metadata:
  policyName: "AWS:VPC:subnet-with-allow-all-egress-acls"
  category: "CSPM"
  stage: "deploy"
  description: "Subnet with Allow All egress NACLs"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Review and restrict Network ACL egress rules to allow only required outbound traffic. Define explicit allow rules for required destination ports and IP ranges. This helps prevent data exfiltration from compromised instances."
