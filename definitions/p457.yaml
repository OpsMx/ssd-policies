id: 457
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:assume-role-lacks-external-id-and-mfa"
  category: "CSPM"
  stage: "deploy"
  description: "Cross-Account AssumeRole Policy Lacks External ID and MFA"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "For cross-account role assumption, require an ExternalId condition to prevent confused deputy attacks, or require MFA for interactive users. Example: 'Condition': {'StringEquals': {'sts:ExternalId': 'unique-id'}}"
