id: 382
type: PolicyDefinition
metadata:
  policyName: "AWS:VPC:subnet-without-flow-log"
  category: "CSPM"
  stage: "deploy"
  description: "Subnet without a Flow Log"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Enable VPC Flow Logs at the VPC level to capture all network traffic. VPC-level flow logs automatically cover all subnets and are easier to manage than subnet-level logs. Send logs to CloudWatch or S3 for analysis."
