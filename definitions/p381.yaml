id: 381
type: PolicyDefinition
metadata:
  policyName: "AWS:VPC:subnet-with-allow-all-ingress-acls"
  category: "CSPM"
  stage: "deploy"
  description: "Subnet with Allow All ingress NACLs"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Review and restrict Network ACL ingress rules to allow only required inbound traffic. Configure explicit allow rules for expected source IP ranges and destination ports, relying on the implicit deny rule to block all other traffic."
