id: 447
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:inline-group-policy-allows-sts-AssumeRole"
  category: "CSPM"
  stage: "deploy"
  description: "Inline group Policy Allows sts:AssumeRole For All Resources"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Modify the inline group policy to restrict sts:AssumeRole to specific role ARNs that group members legitimately need to assume. List the specific role ARNs in the Resource element of the policy statement."
