id: 370
type: PolicyDefinition
metadata:
  policyName: "AWS:EC2:ebs-default-encryption-disabled"
  category: "CSPM"
  stage: "deploy"
  description: "EBS Encryption By Default Is Disabled"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Enable EBS encryption by default in each region using the EC2 console or enable-ebs-encryption-by-default API. All new volumes will be automatically encrypted."
