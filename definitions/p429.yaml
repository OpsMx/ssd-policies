id: 429
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:root-account-no-hardware-mfa"
  category: "CSPM"
  stage: "deploy"
  description: "Root Account without Hardware MFA"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Enable a hardware MFA device for the root account through the IAM console Security Credentials page. Hardware MFA devices provide stronger protection than virtual MFA because they cannot be duplicated or remotely compromised."
