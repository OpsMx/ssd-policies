id: 371
type: PolicyDefinition
metadata:
  policyName: "AWS:EC2:default-security-group-with-rules"
  category: "CSPM"
  stage: "deploy"
  description: "Non-empty Rulesets for Default Security Groups"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Remove all inbound and outbound rules from the default security group. The default security group cannot be deleted but should have no rules. Create custom security groups with appropriate rules for each workload."
