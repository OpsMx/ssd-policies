id: 463
type: PolicyDefinition
metadata:
  policyName: "AWS:IAM:inline-user-policy-allows-sts-AssumeRole"
  category: "CSPM"
  stage: "deploy"
  description: "Inline user Policy Allows sts:AssumeRole For All Resources"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Modify the inline user policy to restrict sts:AssumeRole to specific role ARNs that the user legitimately needs to assume. Convert the inline policy to a managed policy attached via groups for better governance."
