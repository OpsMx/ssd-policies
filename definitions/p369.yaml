id: 369
type: PolicyDefinition
metadata:
  policyName: "AWS:EC2:ebs-snapshot-not-encrypted"
  category: "CSPM"
  stage: "deploy"
  description: "EBS Snapshot Not Encrypted"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Create encrypted copies of unencrypted EBS snapshots using AWS KMS keys, then delete the unencrypted originals. Enable default EBS encryption in account settings to ensure all future snapshots are automatically encrypted."
