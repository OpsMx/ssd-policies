id: 348
type: PolicyDefinition
metadata:
  policyName: "AWS:EC2:security-group-opens-TCP-port-to-all"
  category: "CSPM"
  stage: "deploy"
  description: "Security Group Opens TCP Port to All"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Modify the security group to restrict TCP port access to specific trusted IP addresses, CIDR blocks, or security groups. Open TCP ports to 0.0.0.0/0 expose services to the entire internet. Consider placing public services behind a load balancer or AWS WAF."
