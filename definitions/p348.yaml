id: 348
type: PolicyDefinition
metadata:
  policyName: "AWS:EC2:security-group-opens-TCP-port-to-all"
  category: "CSPM"
  stage: "deploy"
  description: "Security Group Opens TCP Port to All"
  scheduled_policy: false
  scriptId: 345
  variables: ""
  conditionName: ""
  suggestion: "Remove inbound rules that expose TCP ports to all source addresses. Apply source address restrictions to reduce attack surface."
